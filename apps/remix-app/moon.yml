type: 'application'

workspace:
  inheritedTasks:
    exclude: ['buildPackage']

fileGroups:
  app:
    - 'remix.env.d.ts'
    - 'remix.config.*'
  sources:
    - 'app/**/*'
    - 'public/**/*'
    - 'types/**/*'

tasks:
  build:
    command: 'remix build'
    inputs:
      - '@group(app)'
      - '@group(sources)'
    outputs:
      - 'build'

  dev:
    command: 'remix dev'
    local: true

  start:
    command: 'remix-serve ./build'
    deps:
      - 'build'
    local: true
