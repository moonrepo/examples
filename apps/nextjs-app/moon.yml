type: 'application'

workspace:
  inheritedTasks:
    exclude: ['buildPackage']

fileGroups:
  next:
    - 'pages/**/*'
    - 'public/**/*'
    - 'src/**/*'
    - 'next-env.d.ts'
    - 'next.config.*'

tasks:
  build:
    command: 'next build'
    inputs:
      - '@group(next)'

  export:
    command: 'next export -o ./build'
    deps:
      - '~:build'
    inputs:
      - '@group(next)'
    outputs:
      - 'build'

  dev:
    command: 'next dev'
    inputs:
      - '@group(next)'
    local: true

  start:
    command: 'next start'
    inputs:
      - '@group(next)'
    local: true
